services:
  scraper:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - atlantic-data:/data
    # Chromium crashes on Docker's default 64MB /dev/shm.
    shm_size: 1gb
    restart: unless-stopped

volumes:
  atlantic-data:
