[project]
name = "magazine-scraper"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "playwright",
    "ebooklib",
    "beautifulsoup4",
    "lxml",
    "python-dotenv",
    "pillow>=12.1.1",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
    "ty",
    "pre-commit",
]

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.pytest.ini_options]
asyncio_mode = "auto"
